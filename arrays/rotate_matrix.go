package arrays

// Given an NxN matrix, rotate it by 90 degress clockwise in place

// We do not actually need to return it since it is in place but for the sake of
// the testing for the exercise we can.
func RotateMatrix90CW(matrix [][]int) {
	n := len(matrix[0])

	for i := 0; i < n/2; i++ {
	}
}
